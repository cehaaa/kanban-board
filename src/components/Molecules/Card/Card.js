import Base from "./Base/Base";
import Header from "./Header/Header";
import Body from "./Body/Body";

export const Card = {
	Base,
	Header,
	Body,
};

export { Base, Header, Body };

export default Card;
